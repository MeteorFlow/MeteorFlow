{
  // https://nuxt.com/docs/guide/concepts/typescript
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    // "baseUrl": ".", // This must be specified if "paths" is.
    // "paths": {
    //   "lib": ["clients/@types"] // This mapping is relative to "baseUrl"
    // },
    "typeRoots": [
      "./node_modules/@types",
      "./clients/@types"
    ]
  }
}